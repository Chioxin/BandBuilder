import { BandRegistrationForm } from './band-registration-form';

describe('BandRegistrationForm', () => {
  it('should create an instance', () => {
    expect(new BandRegistrationForm()).toBeTruthy();
  });
});
