import { SearchByKeywordPipe } from './search-by-keyword.pipe';

describe('SearchByKeywordPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByKeywordPipe();
    expect(pipe).toBeTruthy();
  });
});
