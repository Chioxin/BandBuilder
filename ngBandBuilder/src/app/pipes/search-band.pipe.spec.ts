import { SearchBandPipe } from './search-band.pipe';

describe('SearchBandPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBandPipe();
    expect(pipe).toBeTruthy();
  });
});
